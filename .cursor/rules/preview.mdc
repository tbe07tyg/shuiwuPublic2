# 方案预览最佳实践

每当Cursor生成多个UI方案（如日历、卡片等）时，应自动将这些方案组件集中引入到 `src/views/设计方案/工作台方案/Preview.vue` 页面，并依次渲染展示。

左侧主菜单栏应固定添加"方案预览"菜单项，点击后通过路由跳转到 `/preview`，即可在新页面对比和预览所有方案。

路由配置应包含 `/preview` 路由，指向上述 Preview.vue 页面。

这样可保证每次生成多个方案后，用户无需手动集成，直接通过菜单栏入口即可一键预览和对比所有方案。
